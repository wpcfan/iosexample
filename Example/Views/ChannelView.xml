<UIView clipsToBounds="true">
    <UIView
        imgUrl="{imageUrls[0]}"
        left="parent.left + 10"
        template="IconLabelButton.xml"
        title="{labels[0]}"
        width="(parent.width - 60) / 5"
    />
    <UIView
        imgUrl="{imageUrls[1]}"
        left="previous.right + 10"
        template="IconLabelButton.xml"
        title="{labels[1]}"
        width="(parent.width - 60) / 5"
    />
    <UIView
        imgUrl="{imageUrls[2]}"
        left="previous.right + 10"
        template="IconLabelButton.xml"
        title="{labels[2]}"
        width="(parent.width - 60) / 5"
    />
    <UIView
        imgUrl="{imageUrls[3]}"
        left="previous.right + 10"
        template="IconLabelButton.xml"
        title="{labels[3]}"
        width="(parent.width - 60) / 5"
    />
    <UIView
        imgUrl="{imageUrls[4]}"
        left="previous.right + 10"
        template="IconLabelButton.xml"
        title="{labels[4]}"
        width="(parent.width - 60) / 5"
    />
</UIView>
